<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <parent>
    <groupId>io.freedriver.autonomy</groupId>
    <artifactId>autonomy</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <modelVersion>4.0.0</modelVersion>
  <artifactId>autonomy-bom</artifactId>
  <packaging>pom</packaging>

  <name>Autonomy BOM</name>
  <description>Bill of Materials for Autonomy artifacts only (api, jpa, quarkus, mqtt-contract, model). Import autonomy-jakarta-bom and/or autonomy-3p-bom separately for APIs and third-party versions.</description>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.freedriver.autonomy</groupId>
        <artifactId>autonomy-api</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>io.freedriver.autonomy</groupId>
        <artifactId>autonomy-jpa</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>io.freedriver.autonomy</groupId>
        <artifactId>autonomy-quarkus</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>io.freedriver.autonomy</groupId>
        <artifactId>autonomy-mqtt-contract</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>io.freedriver.autonomy</groupId>
        <artifactId>autonomy-model</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

</project>
